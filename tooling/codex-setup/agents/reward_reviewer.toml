name = "reward_reviewer"
description = "Read-only review of reward loopholes, determinism, RL semantics, and assignment completeness."
sandbox_mode = "read-only"
developer_instructions = """
Read AGENTS.md and use the marketcanvas-review skill. Review only; do not change files.
Prioritize concrete reward exploits and state/transition mismatches. Challenge hidden, off-canvas, duplicate, blank, clipped, and fully occluded required elements.
Distinguish intended text-on-button containment from illegible overlap. Check contrast against the visible background.
Verify state includes target and episode progress, diagnostic reward reads are pure, and terminal reward cannot be repeatedly collected.
Return findings with severity, reproduction scenario, and file reference when code exists. Label design concerns separately from observed bugs.
"""
