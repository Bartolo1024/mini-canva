prompt: Create a minimal newsletter header containing "Weekly Product Update" and
  a small logo in the upper-left corner. Do not include a CTA.
constraints: # TODO remove it and parse the prompt with some deterministic parser
- id: exists_1
  kind: exists
  hard: true
  selector:
    role: headline
    type: text
- id: text_equals_2
  kind: text_equals
  selector:
    role: headline
  value: Weekly Product Update
- id: exists_3
  kind: exists
  hard: true
  selector:
    role: logo
    type: image
- id: region_4
  kind: region
  selector:
    role: logo
  value: top-left
- id: absent_5
  kind: absent
  hard: true
  selector:
    role: cta
